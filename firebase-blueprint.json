{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile data including VIP status and standard info.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Unique user credentials identifier map ID"
        },
        "username": {
          "type": "string",
          "description": "The custom displayed handle of the gamer"
        },
        "email": {
          "type": "string",
          "description": "Secure email identifier linked to Auth"
        },
        "avatar": {
          "type": "string",
          "description": "Base64 or URL path to profile picture"
        },
        "bio": {
          "type": "string",
          "description": "The user biography writeup"
        },
        "joinDate": {
          "type": "string",
          "description": "Calculated formatted timestamp of join date"
        },
        "vipStatus": {
          "type": "boolean",
          "description": "Indicates if the user has premium VIP tier enabled"
        },
        "favorites": {
          "type": "array",
          "description": "List of game mod IDs flagged as favorites by the user"
        }
      },
      "required": ["userId", "username", "email", "vipStatus"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Secure user profile document accessed by user UID"
    }
  }
}
